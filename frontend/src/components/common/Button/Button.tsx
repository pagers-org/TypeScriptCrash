import { StyledButton } from './Button.styles'

export interface Props {
  to: string;
  cyan: string;
  fullWidth: string;
}

const Button = ({ to, cyan, fullWidth }: Props) => {
  return (
    <>
    </>
  );
}